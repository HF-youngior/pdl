# 企业管理系统数据库实体关系图

## ER图 (Entity Relationship Diagram)

```mermaid
erDiagram
    departments {
        VARCHAR(36) id PK
        VARCHAR(100) name UK
        TEXT description
        TIMESTAMP created_at
    }
    
    users {
        VARCHAR(36) id PK
        VARCHAR(50) username UK
        VARCHAR(255) password
        VARCHAR(100) name
        VARCHAR(100) position
        VARCHAR(36) department_id FK
        ENUM role
        VARCHAR(36) parent_id FK
        BOOLEAN is_active
        TIMESTAMP created_at
        TIMESTAMP last_login_at
    }
    
    company_important_items {
        VARCHAR(36) id PK
        VARCHAR(200) title
        TEXT description
        ENUM priority
        ENUM status
        BOOLEAN is_selected
        TIMESTAMP created_at
        TIMESTAMP deadline
        VARCHAR(36) created_by FK
        VARCHAR(36) updated_by FK
    }
    
    tasks {
        VARCHAR(36) id PK
        VARCHAR(200) title
        TEXT description
        VARCHAR(36) parent_task_id FK
        VARCHAR(36) assignee_id FK
        VARCHAR(100) assignee_name
        VARCHAR(36) department_id FK
        ENUM priority
        ENUM status
        INT progress_percentage
        TIMESTAMP created_at
        TIMESTAMP deadline
        TIMESTAMP completed_at
        VARCHAR(36) created_by FK
        TIMESTAMP start_time
        TIMESTAMP end_time
        VARCHAR(7) color
        VARCHAR(200) location
        BOOLEAN is_all_day
        TEXT special_notes
    }
    
    task_notifications {
        VARCHAR(36) id PK
        VARCHAR(36) task_id FK
        VARCHAR(36) from_user_id FK
        VARCHAR(36) to_user_id FK
        ENUM notification_type
        TEXT message
        BOOLEAN is_read
        TIMESTAMP created_at
    }
    
    personal_logs {
        VARCHAR(36) id PK
        VARCHAR(36) user_id FK
        VARCHAR(200) title
        TEXT content
        VARCHAR(50) category
        ENUM quadrant
        VARCHAR(36) related_task_id FK
        BOOLEAN is_completed
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }
    
    system_logs {
        VARCHAR(36) id PK
        VARCHAR(36) user_id FK
        VARCHAR(100) user_name
        VARCHAR(100) action
        TEXT description
        VARCHAR(50) category
        TIMESTAMP created_at
        JSON metadata
    }

    %% 关系定义
    departments ||--o{ users : "belongs to"
    users ||--o{ users : "reports to"
    users ||--o{ company_important_items : "creates"
    users ||--o{ company_important_items : "updates"
    departments ||--o{ tasks : "contains"
    users ||--o{ tasks : "assigned to"
    users ||--o{ tasks : "creates"
    tasks ||--o{ tasks : "parent-child"
    tasks ||--o{ task_notifications : "generates"
    users ||--o{ task_notifications : "sends"
    users ||--o{ task_notifications : "receives"
    users ||--o{ personal_logs : "writes"
    tasks ||--o{ personal_logs : "relates to"
    users ||--o{ system_logs : "performs"
```

## 表关系说明

### 1. 部门与用户关系
- **一对多关系**: 一个部门可以有多个用户
- **外键**: `users.department_id` → `departments.id`

### 2. 用户层级关系
- **自引用关系**: 用户可以有上级用户
- **外键**: `users.parent_id` → `users.id`
- **层级**: 创始人 → 部门总监 → 团队长 → 员工

### 3. 用户与公司重要事项关系
- **一对多关系**: 一个用户可以创建多个重要事项
- **外键**: `company_important_items.created_by` → `users.id`
- **外键**: `company_important_items.updated_by` → `users.id`

### 4. 部门与任务关系
- **一对多关系**: 一个部门可以有多个任务
- **外键**: `tasks.department_id` → `departments.id`

### 5. 用户与任务关系
- **一对多关系**: 一个用户可以分配多个任务
- **外键**: `tasks.assignee_id` → `users.id`
- **外键**: `tasks.created_by` → `users.id`

### 6. 任务层级关系
- **自引用关系**: 任务可以有父任务
- **外键**: `tasks.parent_task_id` → `tasks.id`

### 7. 任务与通知关系
- **一对多关系**: 一个任务可以生成多个通知
- **外键**: `task_notifications.task_id` → `tasks.id`

### 8. 用户与通知关系
- **一对多关系**: 一个用户可以发送/接收多个通知
- **外键**: `task_notifications.from_user_id` → `users.id`
- **外键**: `task_notifications.to_user_id` → `users.id`

### 9. 用户与个人日志关系
- **一对多关系**: 一个用户可以写多个个人日志
- **外键**: `personal_logs.user_id` → `users.id`

### 10. 任务与个人日志关系
- **一对多关系**: 一个任务可以关联多个个人日志
- **外键**: `personal_logs.related_task_id` → `tasks.id`

### 11. 用户与系统日志关系
- **一对多关系**: 一个用户可以产生多个系统日志
- **外键**: `system_logs.user_id` → `users.id`

## 数据流向图

```mermaid
graph TD
    A[用户登录] --> B[查看任务]
    B --> C[创建/更新任务]
    C --> D[生成通知]
    D --> E[接收通知]
    E --> F[更新任务进度]
    F --> G[记录个人日志]
    G --> H[生成系统日志]
    
    I[部门管理] --> J[用户管理]
    J --> K[任务分配]
    K --> L[进度跟踪]
    L --> M[绩效评估]
```

## 权限层级图

```mermaid
graph TD
    A[管理员 Admin] --> B[创始人 Founder]
    B --> C[部门总监 Department Head]
    C --> D[团队长 Team Leader]
    D --> E[员工 Employee]
    
    A --> |系统管理| F[系统配置]
    B --> |战略决策| G[公司重要事项]
    C --> |部门管理| H[部门任务]
    D --> |团队管理| I[团队任务]
    E --> |执行任务| J[个人任务]
```

## 核心业务流程

### 1. 任务创建流程
```mermaid
sequenceDiagram
    participant U as 用户
    participant T as 任务系统
    participant N as 通知系统
    participant A as 分配用户
    
    U->>T: 创建任务
    T->>T: 保存任务信息
    T->>N: 生成任务分配通知
    N->>A: 发送通知给分配用户
    A->>T: 查看任务详情
```

### 2. 任务更新流程
```mermaid
sequenceDiagram
    participant A as 分配用户
    participant T as 任务系统
    participant N as 通知系统
    participant C as 创建者
    
    A->>T: 更新任务进度
    T->>T: 保存进度信息
    T->>N: 生成进度更新通知
    N->>C: 通知创建者
    C->>T: 查看更新结果
```

这个实体关系图展示了企业管理系统完整的数据库结构和表之间的关系，支持多层级权限管理和任务分配功能。
