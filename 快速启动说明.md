# 快速启动说明

## 🎯 已修复的问题

1. ✅ **Gradle配置错误** - 修复了`settings.gradle.kts`中的编译错误
2. ✅ **UI溢出问题** - 优化了`quadrant_widget.dart`的布局
3. ✅ **Android Gradle Plugin版本** - 升级到8.6.2
4. ✅ **Kotlin版本** - 升级到2.1.0

## 🚀 如何启动应用

### 方法1: 在Windows桌面运行（推荐）

```powershell
cd D:\shixun3\pdl
flutter run -d windows
```

这是最简单的方式，不需要Android模拟器。

### 方法2: 在Android模拟器运行

1. **确保后端运行**:
   ```powershell
   cd D:\shixun3\pdl\backend
   npm start
   ```

2. **在Android Studio启动模拟器**:
   - 打开Android Studio
   - 打开AVD Manager
   - 启动模拟器

3. **运行Flutter应用**:
   ```powershell
   cd D:\shixun3\pdl
   flutter run
   ```

### 方法3: 在Web浏览器运行

```powershell
cd D:\shixun3\pdl
flutter run -d chrome
```

## 🔧 如果遇到Android安装问题

如果Android安装继续失败，请尝试：

1. **清理所有缓存**:
   ```powershell
   flutter clean
   cd android
   .\gradlew clean
   cd ..
   ```

2. **重新获取依赖**:
   ```powershell
   flutter pub get
   ```

3. **重新构建**:
   ```powershell
   flutter build apk
   ```

4. **手动安装APK**:
   ```powershell
   adb install build\app\outputs\flutter-apk\app-debug.apk
   ```

## 🗃️ 数据库配置检查

确保MySQL数据库运行并包含数据：

```powershell
# 检查数据库
mysql -u root -p -e "USE enterprise_management; SHOW TABLES;"

# 如果没有表或数据，运行
cd backend
.\load_more_test_data.bat
```

## 📱 测试账户

- **管理员**: `admin` / `admin123`
- **经理**: `manager` / `manager123`
- **员工**: `employee1` / `employee123`

## 🐛 常见问题

### 1. Android安装失败: "Can't find service: package"

**解决方案**:
1. 重启ADB服务:
   ```powershell
   adb kill-server
   adb start-server
   ```

2. 重启模拟器或设备

3. 使用Web或Windows版本代替

### 2. 端口8080被占用

```powershell
# 查看进程
netstat -ano | findstr :8080

# 结束进程（替换PID为实际进程ID）
taskkill /F /PID <PID>
```

### 3. 数据库连接失败

检查`backend/.env`文件中的数据库配置：
```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Pyx_07091817
DB_NAME=enterprise_management
```

## ✅ 推荐启动方式

最简单的方式是：

```powershell
# 1. 确保后端运行
cd D:\shixun3\pdl\backend
npm start

# 2. 在新终端运行Flutter（Windows版本）
cd D:\shixun3\pdl
flutter run -d windows
```

这样就不需要Android模拟器，启动更快！

