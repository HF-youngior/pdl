# 项目诊断和修复指南

## 当前状态

### ✅ 正常的部分
1. **Flutter环境**: Flutter 3.35.4 正常安装
2. **后端服务器**: 已在端口8080运行 (进程ID: 25104)
3. **依赖安装**: Flutter依赖已安装
4. **可用设备**: 有Android模拟器、Windows桌面和Web浏览器可用

### ⚠️ 发现的问题

#### 1. 代码质量问题（已修复）
- ❌ `lib/screens/login_screen.dart`: 未使用的导入 `task_service.dart`
- ❌ `lib/screens/personal_logs_screen.dart`: 未使用的导入 `intl` 和 `task.dart`

**已修复**: 已删除未使用的导入

#### 2. 需要验证的部分
- ⚠️ MySQL数据库连接状态未确认
- ⚠️ 数据库中是否有测试数据未确认

## 如何运行项目

### 方法1: 启动完整的应用栈

#### 步骤1: 确保后端运行
后端已经在运行（端口8080），如果不小心关闭了，可以运行：
```powershell
cd D:\shixun3\pdl\backend
npm start
```

#### 步骤2: 启动Flutter应用
```powershell
# 在Android模拟器上运行
flutter run

# 或在Windows桌面运行
flutter run -d windows

# 或在Web浏览器运行
flutter run -d chrome
```

### 方法2: 使用批处理文件

项目根目录有一些批处理文件可以帮助启动：
- `启动Android项目.bat` - 启动Android应用
- `start_web_admin.bat` - 启动Web管理端
- `backend/start_enterprise_backend_correct.bat` - 启动后端

## 测试默认账户

根据README.md，可以使用以下账户登录：

### 管理员账户
- 用户名: admin
- 密码: admin123

### 部门老总账户
- 用户名: manager
- 密码: manager123

### 普通员工账户
- 用户名: employee1
- 密码: employee123

## 常见问题排查

### 如果应用启动失败

#### 1. 检查后端是否运行
```powershell
netstat -ano | findstr :8080
```
如果没有任何输出，说明后端未启动。

#### 2. 检查数据库连接
在后端目录运行：
```powershell
cd D:\shixun3\pdl\backend
node test_db_connection.js
```

#### 3. 查看后端日志
检查后端控制台是否有错误信息。

### 如果登录失败

#### 检查后端日志
1. 打开后端运行的控制台
2. 查看是否有数据库连接错误
3. 查看是否有MySQL连接问题

#### 检查数据库状态
```powershell
# 使用MySQL命令行
mysql -u root -p -e "SHOW DATABASES;"
mysql -u root -p -e "USE enterprise_management; SHOW TABLES;"
```

### 如果Flutter应用崩溃

#### 查看详细错误
```powershell
flutter run --verbose
```

#### 清理构建缓存
```powershell
flutter clean
flutter pub get
flutter run
```

## 数据库初始化

如果数据库没有数据，需要导入数据：

### 方法1: 使用批处理文件
```powershell
cd backend
# 根据需要的测试数据选择合适的文件
.\load_more_test_data.bat
# 或
.\load_hr_head_data.bat
```

### 方法2: 手动导入SQL
```powershell
mysql -u root -p enterprise_management < backend/database.sql
mysql -u root -p enterprise_management < backend/create_rich_test_data.sql
```

## 推荐测试步骤

1. ✅ 启动后端服务器
2. ✅ 在Android模拟器上运行Flutter应用
3. ✅ 使用默认账户登录
4. ✅ 测试各个功能模块

## 注意事项

- 后端服务器必须一直运行
- 确保MySQL数据库服务正在运行
- 数据库名称: `enterprise_management`
- API地址: `http://127.0.0.1:8080/api` (本地)
- Android模拟器使用: `http://10.0.2.2:8080/api`

## 下一步

如果你想立即测试应用：

```powershell
# 1. 确保在项目根目录
cd D:\shixun3\pdl

# 2. 启动Flutter应用 (Android模拟器)
flutter run

# 3. 或者启动Windows桌面版本
flutter run -d windows

# 4. 或者启动Web版本
flutter run -d chrome
```

