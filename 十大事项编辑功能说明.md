# 公司十大重要事项编辑功能说明

## 📋 功能概述

本功能实现了公司十大重要事项的完整管理，包括：
- 管理员可以编辑、添加、修改十大事项
- 可以勾选出小于十条事项进行展示
- 其他用户只能查看已选择的十大事项，不能编辑
- Web管理端也支持相同的编辑功能

## 🏗️ 系统架构

### 用户权限层级
- **admin** - 系统管理员：可以编辑十大事项
- **founder** - 创始人：可以编辑十大事项  
- **department_head** - 部门老总：只能查看
- **team_leader** - 团队领导：只能查看
- **employee** - 普通员工：只能查看

### 技术实现
- **前端**: Flutter (Android/iOS) + Web管理端
- **后端**: Node.js + Express
- **数据库**: MySQL
- **认证**: JWT Token

## 🔧 实现的功能

### 1. 后端API接口

#### 新增API端点：
- `GET /api/company-important-items/all` - 获取所有重要事项（用于编辑）
- `PUT /api/company-important-items/batch-select` - 批量更新选择状态
- `POST /api/company-important-items` - 创建重要事项
- `PUT /api/company-important-items/:id` - 更新重要事项
- `DELETE /api/company-important-items/:id` - 删除重要事项

#### 权限控制：
- 所有编辑操作都需要 `admin` 或 `founder` 权限
- 查看操作所有用户都可以访问

### 2. Flutter前端功能

#### 公司重要事项查看页面 (`company_important_screen.dart`)
- 显示已选择的十大重要事项
- 管理员和创始人可以看到编辑按钮
- 其他用户只能查看，无编辑权限

#### 公司重要事项编辑页面 (`company_important_edit_screen.dart`)
- 显示所有重要事项供选择
- 支持多选，最多选择10个
- 实时显示已选择数量
- 保存后自动刷新主页面

#### API服务 (`api_service.dart`)
- `getAllImportantItems()` - 获取所有事项
- `batchUpdateImportantItemsSelection()` - 批量更新选择状态
- `createImportantItem()` - 创建事项
- `updateImportantItem()` - 更新事项
- `deleteImportantItem()` - 删除事项

### 3. Web管理端功能

#### 重要事项管理页面
- 添加重要事项按钮
- 编辑十大事项按钮（批量选择）
- 刷新按钮
- 完整的CRUD操作界面

#### 模态框功能
- 添加重要事项模态框
- 编辑重要事项模态框
- 批量选择十大事项模态框

## 📊 数据库设计

### company_important_items 表结构
```sql
CREATE TABLE company_important_items (
  id VARCHAR(36) PRIMARY KEY,
  title VARCHAR(200) NOT NULL,
  description TEXT,
  priority ENUM('p0', 'p1', 'p2', 'p3') DEFAULT 'p1',
  status ENUM('pending', 'in_progress', 'completed', 'cancelled') DEFAULT 'pending',
  is_selected BOOLEAN DEFAULT FALSE,  -- 关键字段：是否被选为十大事项
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  deadline TIMESTAMP NULL,
  created_by VARCHAR(36) NOT NULL,
  updated_by VARCHAR(36) NULL
);
```

## 🚀 使用方法

### 1. 启动系统
```bash
# 启动后端服务
cd backend
node server_enterprise.js

# 启动Flutter应用
flutter run

# 启动Web管理端
# 在浏览器中打开 web_admin/index.html
```

### 2. 管理员操作流程

#### Flutter移动端：
1. 使用admin或founder账号登录
2. 进入"公司重要展示"页面
3. 点击右上角编辑按钮
4. 选择最多10个重要事项
5. 点击保存按钮

#### Web管理端：
1. 使用admin或founder账号登录
2. 进入"重要事项"管理页面
3. 点击"编辑十大事项"按钮
4. 选择最多10个重要事项
5. 点击"保存选择"按钮

### 3. 普通用户操作流程
1. 使用普通用户账号登录
2. 进入"公司重要展示"页面
3. 只能查看已选择的十大重要事项
4. 无法进行编辑操作

## 🧪 测试功能

运行测试脚本验证功能：
```bash
node test_important_items.js
```

测试内容包括：
- 用户登录认证
- 创建重要事项
- 获取所有重要事项
- 批量选择重要事项
- 获取已选择的重要事项
- 更新重要事项
- 删除重要事项

## 🔒 安全特性

1. **权限控制**：所有编辑操作都需要管理员权限
2. **JWT认证**：所有API调用都需要有效的JWT token
3. **数据验证**：前后端都有数据验证机制
4. **事务处理**：批量更新使用数据库事务确保数据一致性

## 📱 界面特性

### Flutter移动端
- 现代化Material Design界面
- 直观的选择界面
- 实时状态反馈
- 响应式布局

### Web管理端
- Bootstrap 5响应式设计
- 模态框交互
- 实时数据更新
- 用户友好的操作界面

## 🎯 核心功能总结

| 功能 | 管理员/创始人 | 其他用户 |
|------|-------------|----------|
| 查看十大事项 | ✅ | ✅ |
| 编辑十大事项 | ✅ | ❌ |
| 添加新事项 | ✅ | ❌ |
| 修改事项内容 | ✅ | ❌ |
| 删除事项 | ✅ | ❌ |
| 批量选择 | ✅ | ❌ |

## 🔧 技术要点

1. **前端权限控制**：通过用户角色判断是否显示编辑按钮
2. **后端权限控制**：使用JWT认证和角色检查中间件
3. **数据库权限**：通过`is_selected`字段控制哪些事项对外展示
4. **数量限制**：前端限制最多选择10个事项
5. **实时更新**：编辑后自动刷新显示内容

这个实现完全满足了您的需求：管理员可以编辑和勾选十大事项，其他用户只能查看已勾选的事项。
