# PDL 项目接口文档

## 📋 项目概述

PDL (Personal Digital Life) 个人数字化生活管理系统提供完整的前后端接口服务，支持任务管理、MBTI 测试、AI 助手等功能。

## 🚀 API 基础信息

- **基础 URL**: http://localhost:8080
- **端口**: 8080
- **认证**: JWT Bearer Token

## 📡 主要接口列表

### 1. 重要事项管理

#### 获取重要事项列表
- **端点**: `GET /api/important-items`
- **描述**: 获取所有标记为已选中的重要事项
- **返回格式**: JSON
- **示例响应**:
  ```json
  [
    {
      "id": "1",
      "title": "项目完成",
      "description": "完成 PDL 项目开发",
      "is_selected": true,
      "created_by_name": "管理员",
      "updated_by_name": "管理员",
      "created_at": "2025-12-21T10:30:00Z"
    }
  ]
  ```

#### 获取公司重要事项
- **端点**: `GET /api/company-important-items`
- **描述**: 获取已选中用于展示的公司重要事项
- **返回格式**: JSON

### 2. 用户管理

#### 用户登录
- **端点**: `POST /api/login`
- **描述**: 用户身份验证
- **参数**:
  - `username`: 用户名
  - `password`: 密码
- **返回格式**: JSON

#### 用户注册
- **端点**: `POST /api/register`
- **描述**: 新用户注册
- **参数**:
  - `username`: 用户名
  - `password`: 密码
  - `email`: 邮箱（可选）

### 3. 任务管理

#### 获取任务列表
- **端点**: `GET /api/tasks`
- **描述**: 获取用户任务列表
- **参数**:
  - `user_id`: 用户 ID
  - `status`: 任务状态（可选）
  - `priority`: 优先级（可选）

#### 创建任务
- **端点**: `POST /api/tasks`
- **描述**: 创建新任务
- **参数**:
  - `title`: 任务标题
  - `description`: 任务描述
  - `due_date`: 截止日期
  - `priority`: 优先级

#### 更新任务
- **端点**: `PUT /api/tasks/:id`
- **描述**: 更新任务信息

#### 删除任务
- **端点**: `DELETE /api/tasks/:id`
- **描述**: 删除任务

### 4. MBTI 测试

#### 获取 MBTI 测试
- **端点**: `GET /api/mbti-tests`
- **描述**: 获取 MBTI 测试题目

#### 提交 MBTI 测试结果
- **端点**: `POST /api/mbti-results`
- **描述**: 提交用户 MBTI 测试结果

### 5. 数据统计

#### 获取用户统计信息
- **端点**: `GET /api/statistics/user/:id`
- **描述**: 获取用户的统计信息

#### 获取任务统计
- **端点**: `GET /api/statistics/tasks`
- **描述**: 获取任务相关统计数据

## 🔐 认证

所有受保护的 API 端点需要在请求头中包含 JWT Token：

```
Authorization: Bearer <your-jwt-token>
```

## ⚠️ 错误码说明

- `400`: 请求参数错误
- `401`: 未授权
- `403`: 禁止访问
- `404`: 资源未找到
- `500`: 服务器内部错误
