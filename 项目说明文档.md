# PDL 项目总说明文档

## 📋 项目概述

PDL (Personal Digital Life) 是一个个人数字化生活管理系统，集成了任务管理、MBTI 性格测试、AI 助手等功能，提供完整的前后端解决方案。

## 📁 项目结构

整理后的项目目录结构如下：

```
📁 pdl/
├── 📁 android/              # Android 移动端代码
├── 📁 android_old/          # 旧版 Android 代码备份
├── 📁 assets/               # 静态资源文件
├── 📁 backend/              # Node.js 后端服务
├── 📁 docs/                 # 文档目录（保留）
├── 📁 ios/                  # iOS 移动端代码
├── 📁 lib/                  # Flutter 前端源代码
├── 📁 linux/                # Linux 平台代码
├── 📁 macos/                # macOS 平台代码
├── 📁 project_docs_scripts/ # 整理后的文档和脚本
├── 📁 web/                  # Web 前端代码
├── 📁 web_admin/            # Web 管理端
├── 📁 windows/              # Windows 平台代码
├── package-lock.json        # Node.js 依赖锁
├── package.json             # Node.js 项目配置
├── pdl.db                   # SQLite 数据库文件
├── pubspec.lock             # Flutter 依赖锁
├── pubspec.yaml             # Flutter 项目配置
├── README.md                # 快速说明
├── sonar-project.properties # SonarQube 配置
└── 项目说明文档.md           # 本文件
```

## 🚀 快速开始

### 1. 环境准备

确保已安装：
- Node.js 14+ 及 npm
- Flutter SDK
- 适当的数据库环境（SQLite 已内置）

### 2. 安装依赖

```bash
# 安装后端依赖
cd backend
npm install

# 安装 Flutter 依赖（在项目根目录）
flutter pub get
```

### 3. 启动服务

```bash
# 启动后端服务
cd backend
node server_enterprise.js  # 或执行相应的启动脚本

# 启动 Flutter 应用（新终端窗口）
flutter run
```

## 📝 详细运行指南

### 后端服务启动

1. **配置环境**：
   - 复制 `backend/env.example` 为 `backend/.env`
   - 根据需要修改环境变量

2. **启动服务**：
   ```bash
   cd backend
   node server_enterprise.js
   ```

3. **验证连接**：
   - 访问 http://localhost:8080 验证服务

### 前端应用启动

1. **连接设备或启动模拟器**
2. **运行 Flutter 应用**：
   ```bash
   flutter run
   ```

## 📚 功能模块

- **任务管理系统**：支持多视图（日/周/月）、跨天任务、提醒
- **MBTI 测试**：完整的性格测试功能和报告
- **AI 助手**：智能建议和数据分析
- **时间管理**：时区处理、日程规划
- **数据可视化**：各种图表和统计

## 🛠️ 开发工具

- **代码质量**：SonarQube 集成
- **测试**：Mocha 测试框架
- **构建**：Flutter Build

## 📁 文档与脚本

所有的文档、批处理文件和辅助脚本已整理到 `project_docs_scripts` 文件夹中，包括：
- 安装指南
- 修复脚本
- 测试文件
- SonarQube 工具
- 构建脚本

## 🤝 贡献

请阅读相关文档了解如何贡献代码。

## 📞 联系方式

如有问题，请参考项目文档或联系开发团队。
