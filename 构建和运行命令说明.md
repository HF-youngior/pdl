# 构建和运行命令说明

## 问题描述

Gradle 构建成功生成了 APK 文件，但 Flutter 工具无法找到它。APK 文件实际存在于：
- `android\app\build\outputs\apk\debug\app-debug.apk`
- `android\app\build\outputs\flutter-apk\app-debug.apk`

## 解决方案

### 方案 1: 使用 Gradle 直接构建（推荐）

#### 命令行方式
```powershell
# 进入 android 目录
cd android

# 使用 Gradle 构建 APK
.\gradlew.bat assembleDebug

# 返回项目根目录
cd ..
```

#### 批处理脚本方式
```bash
# 双击运行
构建APK-使用Gradle.bat
```

### 方案 2: 使用 Flutter run（推荐用于开发）

```powershell
# 直接运行应用（会自动构建并安装到设备/模拟器）
flutter run
```

### 方案 3: 手动复制 APK（如果 Flutter 工具需要）

如果 Flutter 工具期望在 `build` 目录下找到 APK，可以手动复制：

```powershell
# 创建 build 目录（如果不存在）
New-Item -ItemType Directory -Force -Path "build\app\outputs\apk\debug"

# 复制 APK 文件
Copy-Item "android\app\build\outputs\apk\debug\app-debug.apk" -Destination "build\app\outputs\apk\debug\app-debug.apk"
```

## 常用命令

### 1. 清理构建缓存
```powershell
# 停止 Gradle 守护进程
cd android
.\gradlew.bat --stop
cd ..

# 清理 Flutter 缓存
flutter clean

# 清理 Android 构建缓存
Remove-Item -Recurse -Force "android\app\build" -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "android\build" -ErrorAction SilentlyContinue
```

### 2. 重新构建
```powershell
# 获取依赖
flutter pub get

# 使用 Gradle 构建
cd android
.\gradlew.bat assembleDebug
cd ..

# 或使用 Flutter 运行
flutter run
```

### 3. 检查 APK 文件
```powershell
# 检查 APK 是否存在
Test-Path "android\app\build\outputs\apk\debug\app-debug.apk"

# 查看 APK 文件信息
Get-Item "android\app\build\outputs\apk\debug\app-debug.apk" | Select-Object FullName, Length, LastWriteTime
```

## 完整工作流程

### 开发环境
```powershell
# 1. 清理（如果需要）
flutter clean
flutter pub get

# 2. 运行应用
flutter run
```

### 构建 APK
```powershell
# 1. 清理
flutter clean
flutter pub get

# 2. 使用 Gradle 构建
cd android
.\gradlew.bat assembleDebug
cd ..

# 3. 验证 APK
Test-Path "android\app\build\outputs\apk\debug\app-debug.apk"
```

## 问题排查

### 如果构建失败
1. **检查 Gradle 版本**
   ```powershell
   cd android
   .\gradlew.bat --version
   ```

2. **检查 Android SDK**
   ```powershell
   # 检查 local.properties
   Get-Content android\local.properties
   ```

3. **清理并重建**
   ```powershell
   # 运行清理脚本
   .\清理构建并重建.bat
   ```

### 如果 APK 无法找到
1. **检查构建输出目录**
   ```powershell
   Get-ChildItem "android\app\build\outputs" -Recurse -Filter "*.apk"
   ```

2. **检查构建日志**
   ```powershell
   cd android
   .\gradlew.bat assembleDebug --info
   ```

## 相关文件

- `构建APK-使用Gradle.bat` - 使用 Gradle 构建 APK 的脚本
- `清理构建并重建.bat` - 清理构建缓存的脚本
- `快速修复构建问题.bat` - 快速修复脚本
- `彻底清理构建缓存.bat` - 彻底清理脚本

## 注意事项

1. **APK 文件位置**：APK 文件通常位于 `android\app\build\outputs\apk\debug\app-debug.apk`
2. **Flutter 工具问题**：如果 Flutter 工具无法找到 APK，可以使用 Gradle 直接构建
3. **开发环境**：开发时建议使用 `flutter run`，它会自动处理构建和安装
4. **构建时间**：首次构建可能需要较长时间，后续构建会更快

